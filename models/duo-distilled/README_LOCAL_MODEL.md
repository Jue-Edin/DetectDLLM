Place the local `s-sahoo/duo-distilled` checkpoint files in this directory.

This folder is intentionally kept lightweight in git. The actual weight file
`model.safetensors` is expected to live only on local machines or servers.
